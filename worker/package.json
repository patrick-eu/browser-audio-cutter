{
  "name": "snipaudio-worker",
  "private": true,
  "description": "Edge Worker for snipaudio.com agent resources: Markdown negotiation, read-only API, MCP, discovery documents",
  "scripts": {
    "build": "node agent/build.mjs",
    "test": "node agent/test.mjs"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "wrangler": "^4.131.1",
    "zod": "^3.25.0"
  }
}
